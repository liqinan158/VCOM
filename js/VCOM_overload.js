var custom_content = `<meta charset='utf-8'/><div class='VCOM'><div class="headword">overload</div><div class="blurb"><p class="short">To <i>overload</i> is to load an excessive amount in or on something, such as an <i>overload</i> of electricity which shorts out the circuits. <i>Overloading</i> causes a "Too much!" situation.</p><p class="long">To overload is to push something or someone too far. A supervisor can overload an employee by assigning too much work. People can overload a bus if there are too many of them. A fuse will blow if too many appliances overload the circuits; this is called an <i>overload</i> (the noun form). When something gets overloaded, it usually stops working. Hair can overload or clog a sink, which will stop draining water. When something gets overloaded, there's more of something than it can handle.</p></div></div>`;
window.parent.postMessage(custom_content,'*');