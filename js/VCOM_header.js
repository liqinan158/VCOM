var custom_content = `<meta charset='utf-8'/><div class="headword">header</div><div class="blurb"><p class="short">A <i>header</i> is a separate bit of text at the top of a printed page. A <i>header</i> might be the title of the novel you're reading, which is repeated on each page of the book.</p><p class="long">One kind of header — also called a "page <i>header</i>" — is a chapter title, which is often printed at the top of every page. It's called a <i>header</i> because it's printed at the top, or head, of the page. When you format a long paper for school, you may include page numbers as headers. In any case, a header is set apart from the main text of a document and is sometimes printed in a larger or bolder font.</p></div>`;
window.parent.postMessage(custom_content,'*');