var custom_content = `<meta charset='utf-8'/><div class="headword">iteration</div><div class="blurb"><p class="short">An <i>iteration</i> is a repetition of something. Your nagging friend's message might be lost on you after the umpteenth <i>iteration</i>.</p><p class="long">The noun <i>iteration</i> comes from the Latin verb <i>iterum</i>, which means "again," which makes a lot of sense when you consider the meaning of the word, "the act of repeating." Don’t get confused by the similar word <i>reiteration</i>, which essentially means the same thing. Either word is proper to use. Although when you think about it, the "re" means again anyway, so that word actually means "to repeat again," which is in itself a little redundant, don't you think?</p></div>`;
window.parent.postMessage(custom_content,'*');