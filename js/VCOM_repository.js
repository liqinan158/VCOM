var custom_content = `<meta charset='utf-8'/><div class="headword">repository</div><div class="blurb"><p class="short">Think of a <i>repository</i> as a storehouse, a place where things are kept until needed. A silo is a <i>repository</i> for animal feed, and a trivia buff's mind is a <i>repository</i> for useless information.</p><p class="long">The noun <i>repository</i> has a somewhat formal ring to it — why not just call a storehouse a storehouse, right? Think of a <i>repository</i> as somewhat more important and imposing than just a place to dump stuff. It has more of an air of authority, like the things stored there could change the world. Consider nuclear waste — would you rather store it in a dump or a repository? I don’t know about you, but I'd prefer the latter—sounds safer.</p></div>`;
window.parent.postMessage(custom_content,'*');