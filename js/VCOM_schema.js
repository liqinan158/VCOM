var custom_content = `<meta charset='utf-8'/><div class="headword">schema</div><div class="blurb"><p class="short">If you were just commissioned to modernize your town's library, the first thing you'll make is a <i>schema</i>: an outline of your plan for reinventing libraries.</p><p class="long"><i>Schema</i> sounds like <i>scheme</i>, but they have different nuances. You scheme to snatch a cookie from the jar. A schema is more of a master plan. It can also just be an organized vision. If you have a master concept about how the world works, that's your world organization schema. Someone with an insanely big idea about how to change the world has "a grand schema." Now she just needs to make a practical schema for actually making that change.</p></div>`;
window.parent.postMessage(custom_content,'*');