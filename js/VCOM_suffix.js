var custom_content = `<meta charset='utf-8'/><div class='VCOM'><div class="headword">suffix</div><div class="blurb"><p class="short">In linguistics, the <i>suffix</i> is part of the word that's added onto the end, like the <i>-ish</i> in <i>foolish</i>.</p><p class="long">When a suffix is added to a word, it changes the form, like when adding the suffix <i>-y</i> turns the noun <i>soap</i> into the adjective <i>soapy</i>. You can also use <i>suffix</i> as a verb. If you suffix the word <i>cake</i> with <i>-less</i>, you end up cakeless, which is pretty sad, especially if it's your birthday.</p></div></div>`;
window.parent.postMessage(custom_content,'*');