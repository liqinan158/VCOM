var custom_content = `<meta charset='utf-8'/><div class='VCOM'><div class="headword">idempotent</div><div class="blurb"><p class="short">In mathematics, a number that is <i>idempotent</i> keeps the same value when multiplied by itself, no matter how many times the function is applied.</p><p class="long">The word <i>idempotent</i> comes from the Latin words <i>idem</i>, which means "the same," and <i>potent</i>, from <i>potentem</i>, which means "power." For example, one times one always equals one no matter how many times you multiply it. If you press the “pause” button on a video player many times with no new results, the button is idempotent.  Can we finish the movie now?</p></div></div>`;
window.parent.postMessage(custom_content,'*');