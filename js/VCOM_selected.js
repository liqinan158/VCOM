var custom_content = `<meta charset='utf-8'/><div class='VCOM'><div class="headword">selected</div><div class="blurb"><p class="short">Anything <i>selected</i> has been picked. A <i>selected</i> prize has been chosen instead of other prizes.</p><p class="long">To select is to pick something. When you shop, you make lots of selections. Something selected has been consciously chosen instead of other things. A selected duty is one an employee chooses to do, not one they're ordered to do. A selected penalty is one that is picked, not forced on someone. In a sports draft, the #1 pick is a selected player who was picked before everyone else.</p></div></div>`;
window.parent.postMessage(custom_content,'*');