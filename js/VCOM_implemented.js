var custom_content = `<meta charset='utf-8'/><div class="headword">implemented</div><div class="blurb"><p class="short">When something is <i>implemented</i>, it's put in place or enforced.  If you hear a rumor at work that all employees must wear hats with propellers, you'll probably be relieved to learn that it's not an <i>implemented</i> policy — at least, not yet.</p><p class="long">The Latin word <i>implementum</i> means "filling up" or "fulfillment." A  decision, plan, or policy is like an empty idea until it's implemented. An implemented rule, on the other hand, is in full force, and you'll have to stick to it. When a law has been suggested, officially proposed, voted on, and finally written into the law books, you can describe it as <i>implemented</i>.</p></div>`;
window.parent.postMessage(custom_content,'*');