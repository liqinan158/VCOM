var custom_content = `<meta charset='utf-8'/><div class="headword">parameter</div><div class="blurb"><p class="short">A <i>parameter</i> is a limit. In mathematics a <i>parameter</i> is a constant in an equation, but <i>parameter</i> isn’t just for math anymore: now any system can have parameters that define its operation. You can set <i>parameters</i> for your class debate.</p><p class="long"><i>Parameter</i> comes from a combination of the Greek word <i>para-</i>, meaning “beside,” and <i>metron</i>, meaning “measure.” The natural world sets certain parameters, like gravity and time. In court, the law defines the parameters of legal behavior. <i>Parameter</i> and <i>perimeter</i> are similar, but a perimeter is the physical distance around an object, while a parameter can contain or define something either physically or abstractly: before you marry, prepare for the parameters of monogamy.</p></div>`;
window.parent.postMessage(custom_content,'*');