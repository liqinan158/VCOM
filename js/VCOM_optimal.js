var custom_content = `<meta charset='utf-8'/><div class='VCOM'><div class="headword">optimal</div><div class="blurb"><p class="short">Something that's <i>optimal</i> is the best or most favorable for a given situation. Flowers? Check. Chocolates? Check. Dinner reservation? Check. Sounds like you have the <i>optimal</i> conditions for a successful anniversary date.</p><p class="long">There's an implication with <i>optimal</i> that certain restrictions exist in a given situation, so that what is optimal isn't necessarily the ideal scenario — it's the best one given those limitations. For example, optimal population levels for a species are not the highest levels — instead they're the best levels that a particular environment can sustain. Often the word <i>optimum</i> is used interchangeably with <i>optimal</i>.</p></div></div>`;
window.parent.postMessage(custom_content,'*');