var custom_content = `<meta charset='utf-8'/><div class="headword">projection</div><div class="blurb"><p class="short">When you push something away from a central structure, that's called <i>projection</i>.  If you predict that the Jets will win tomorrow's game, that's a projection, too — you're pushing your mind away from the present and into the future.</p><p class="long">The Jets and <i>projection</i> actually have a common linguistic ancestor. Both the <i>ject</i> in <i>projection</i> and the word <i>jet</i> come from the Latin root <i>jactus</i>, which means "throw." A jet plane throws itself — or projects itself — away from a central structure (the Earth) and into the air.</p></div>`;
window.parent.postMessage(custom_content,'*');