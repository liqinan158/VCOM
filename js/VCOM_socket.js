var custom_content = `<meta charset='utf-8'/><div class='VCOM'><div class="headword">socket</div><div class="blurb"><p class="short">However many people it takes to change a light bulb, at least one of them will have to unscrew the old bulb from the <i>socket</i> and replace it with a new one. A hollow or cavity that you insert something into is a <i>socket</i>.</p><p class="long">Many people also call electrical outlets (like the one you plug a toaster into) <i>sockets</i>. This makes sense because you are inserting a plug into it—some purists argue that it has to be called an <i>outlet</i> because power comes out  of it, but it's a perfectly good way to use the word. There are also anatomical sockets in our bodies, like eye sockets into which eyeballs fit neatly, and ball-and-socket joints, like hips and shoulders.</p></div></div>`;
window.parent.postMessage(custom_content,'*');