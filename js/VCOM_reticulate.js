var custom_content = `<meta charset='utf-8'/><div class='VCOM'><div class="headword">reticulate</div><div class="blurb"><p class="short">To <i>reticulate</i> is to form a type of net or network.</p><p class="long">Reticulating is an activity that creates a net or network. Linking a group of computers into a shared network reticulates them. A spider spinning a web is doing a form of reticulating. A businessperson making connections with many colleagues is reticulating by creating a social network with them. The key to any form of reticulating is that it makes connections: it links people or things in a type of net.</p></div></div>`;
window.parent.postMessage(custom_content,'*');