var custom_content = `<meta charset='utf-8'/><div class='VCOM'><div class="headword">naive</div><div class="blurb"><p class="short">No one likes being called <i>naive</i>, since it means you lack sophistication or street smarts.</p><p class="long">A person who is too trusting can be criticized for being overly naive: "You're so <i>naive</i>, you think that map you bought online will really lead you to buried treasure!" <i>Naive</i> shares the same root as <i>native</i>, and originally meant "natural" or "not artificial." It can still be used in a more positive meaning when describing a charming lack of artificiality, as in "the <i>naive</i> style of folk art made by an untrained painter."</p></div></div>`;
window.parent.postMessage(custom_content,'*');