var custom_content = `<meta charset='utf-8'/><div class="headword">bundle</div><div class="blurb"><p class="short">A <i>bundle</i> is a package of things wrapped together. To wrap things together in a compact way is to <i>bundle</i> them. A baby wrapped up in a blanket is a <i>bundle</i> of joy, and if it’s cold outside, <i>bundle</i> up!</p><p class="long"><i>Bundle</i> comes from the Middle Dutch word for <i>bind</i>, which is what you do when you bundle stuff — you bind it together. It also means to wrap in warm clothes. This is a word for things that are tightly packed together. If you get a package in the mail, it's a bundle. If you have knickknacks all over the place, you'll need to bundle them when you move by putting them in a box.</p></div>`;
window.parent.postMessage(custom_content,'*');