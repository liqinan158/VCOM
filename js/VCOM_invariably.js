var custom_content = `<meta charset='utf-8'/><div class='VCOM'><div class="headword">invariably</div><div class="blurb"><p class="short"><i>Invariably</i> describes things that don't change and never vary — they're predictable. Many people <i>invariably</i> start each morning with a hot cup of coffee.</p><p class="long">This adverb is useful when discussing things that happen all the time or don't change. In football, teams that win the Super Bowl invariably have a good defense. In schools, many students invariably struggle with writing. Invariably, the weather is hard to predict. In a lot of cases, <i>invariably</i> means always or almost always. You can count on things that are <i>invariable</i>.</p></div></div>`;
window.parent.postMessage(custom_content,'*');