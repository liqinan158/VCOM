var custom_content = `<meta charset='utf-8'/><div class='VCOM'><div class="headword">back-to-back</div><div class="blurb"><p class="short"><i>Back-to-back</i> things come right after each other. Two <i>back-to-back</i> baseball games are played in a row.</p><p class="long"><i>Back-to-back</i> has been used to describe houses that are literally back-to-back with no separation. These days, back-to-back events happen consecutively. A baseball player with back-to-back home runs hit two in a row. A President with back-to-back victories won two elections in a row. There's no separation between back-to-back things, like how if your back was pressed against someone else’s back, nothing could fit in between. Sometimes people use the term <i>back-to-back-to-back</i> for things that happen three times in a row.</p></div></div>`;
window.parent.postMessage(custom_content,'*');