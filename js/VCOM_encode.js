var custom_content = `<meta charset='utf-8'/><div class="headword">encode</div><div class="blurb"><p class="short">To <i>encode</i> something is to put it into a coded form. During World War II, countries would <i>encode</i> messages so that if they were intercepted, their enemies couldn't understand them.</p><p class="long">There are various reasons why information might need to be written in code: spies and secret agents need to encode messages so that only people who know the code can decipher their meanings. Computer languages are also thought of as codes, and when you convert information into one of these languages, you also encode it — although this modern meaning is usually shortened to the verb <i>code</i>.</p></div>`;
window.parent.postMessage(custom_content,'*');