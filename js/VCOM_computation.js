var custom_content = `<meta charset='utf-8'/><div class='VCOM'><div class="headword">computation</div><div class="blurb"><p class="short"><i>Computation</i> is what you do when you calculate something, like a simple math problem or something involving quantum mechanics or —  worse — even your taxes.</p><p class="long">People have invented all kinds of machines to help with computation, from the abacus to the computer, and, in fact, when computers were first invented (and when one computer could easily fill a large room), using a computer was called <i>computation</i>. Much of what made computers so revolutionary was that in a relatively short time they could do a complex mathematical computation that would take a human many lifetimes to do.</p></div></div>`;
window.parent.postMessage(custom_content,'*');